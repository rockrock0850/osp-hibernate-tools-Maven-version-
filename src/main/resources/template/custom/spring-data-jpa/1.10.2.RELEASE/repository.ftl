${pojo.getPackageDeclaration()}/*${pojo.getClassJavaDoc(pojo.getDeclarationName() + " generated by Hibernate tools ext(Spring Data Repository)", 0)} */<#assign idProperty>	<#foreach property in pojo.getAllPropertiesIterator()>		<#if pojo.hasIdentifierProperty()>			<#if property.equals(clazz.identifierProperty)>			 ${pojo.getJavaTypeName(property, jdk5)}			</#if>		</#if>	</#foreach></#assign>import org.springframework.data.jpa.repository.JpaRepository; public interface ${pojo.getDeclarationName()}Repository extends JpaRepository<${pojo.getDeclarationName()}, ${idProperty}> {}